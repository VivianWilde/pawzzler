import gui
import gamestate



def main():
    gui = GUI()
    #gui needs to 1) show a screen with options for new game/from saved game
    # 2) on new game, call Gamestate.new_game(self) -> return gamestate

    # 3) wait for player input.